import React, { useState } from 'react'

const TempChild = ({onpress}) => {
 
    return (
        <div>
            <button onClick={onpress}>click</button>
        </div>
    )
}

export default TempChild
